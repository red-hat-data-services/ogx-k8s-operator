name: Build Operator Image

on:
  push:
    branches:
       - odh

permissions:
  actions: write
  contents: read

jobs:
  build-latest-image:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744

      - name: Set up Go 1.24
        uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version-file: go.mod

      - name: Login to Quay.io
        uses: docker/login-action@465a07811f14bebb1938fbed4728c6a1ff8901fc
        with:
          registry: quay.io
          username: ${{ secrets.APP_QUAY_USERNAME }}
          password: ${{ secrets.APP_QUAY_TOKEN }}

      - name: Build and push odh image
        run: make image-build image-push -e IMG=quay.io/opendatahub/odh-ogx-k8s-operator:odh

      - name: Build and push odh ogx-module image
        run: make ogx-module-image -e OGX_MODULE_IMG=quay.io/opendatahub/odh-ogx-module-operator:odh
