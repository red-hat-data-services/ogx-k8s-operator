apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ogx-k8s-operator-tls-profile
rules:
- apiGroups:
  - config.openshift.io
  resourceNames:
  - cluster
  resources:
  - apiservers
  verbs:
  - get
- apiGroups:
  - config.openshift.io
  resources:
  - apiservers
  verbs:
  - list
  - watch
