apiVersion: ogx.io/v1beta1
kind: OGXServer
metadata:
  name: test-rh-override-inference
spec:
  distribution:
    name: rh
  providers:
    inference:
      remote:
        vllm:
        - endpoint: https://my-vllm:8000
