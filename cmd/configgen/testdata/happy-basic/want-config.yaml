version: "2"
distro_name: starter
apis:
  - inference
providers:
  inference:
    - provider_id: remote-vllm
      provider_type: remote::vllm
      config:
        base_url: https://my-vllm:8000
registered_resources:
  models:
    - model_id: llama3
      model_type: llm
      provider_id: remote-vllm
server:
  port: 8321
